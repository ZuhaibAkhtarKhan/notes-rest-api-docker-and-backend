include:
  - path: notebooks-backend/compose.yaml
  - path: notes-backend/compose.yaml # we can do it without the path keywords

services:
  reverse-proxy:
    image: nginx:1.27.0
    volumes:
      - type: bind
        source: ./reverse-proxy/nginx.conf
        target: /etc/nginx/nginx.conf
    ports:
      - 5000:80
    networks:
      - notes-app-net
    depends_on:
      - notebooks
      - notes

networks:
  notes-app-net: